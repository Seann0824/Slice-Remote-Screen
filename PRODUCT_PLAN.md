# Slice Remote Screen 产品规划方案

关联文档：[技术方案](./TECHNICAL_DESIGN.md) · [GitHub 调研](./GITHUB_RESEARCH.md)

## 1. 一句话定义

提供一套完整远程控制底座，先把整台电脑按应用隔离展示，再把单个应用的实时画面切分为适合手机直接操作的交互区域。

产品不是阉割版远程桌面，也不是 Codex 专用客户端。完整桌面与完整应用远控始终保留；“快捷 App”的主体是多个实时、可交互的画面区域，不是一排宏按钮。

## 2. 要解决的问题

用户离开电脑后，经常只需要完成少量明确操作，例如：

- 查看 Codex 当前是否仍在运行；
- 确认、拒绝或继续一个任务；
- 向输入框发送一句补充指令；
- 点击应用中的固定按钮；
- 执行一个快捷键或预设操作序列；
- 在快捷操作失效或需要复杂操作时，无缝展开为完整应用窗口或完整桌面。

现有远程桌面把整套桌面 UI 原样塞进手机，信息过载、点击困难、链路成本高。官方移动客户端又只能覆盖自家产品，不能统一控制电脑上的其他应用。

## 3. 产品边界

### V1 要做

- 仅支持 macOS；
- 单用户、单台 Mac；
- Mac 常驻服务与手机 Web/PWA 控制端；
- 保留完整桌面、单个应用窗口和局部区域三种远控视图；
- 三种视图共用同一套画面采集、输入注入、连接、认证和权限能力；
- 支持从实时交互区域逐级展开到完整应用和完整桌面；
- 手动框选应用中的实时交互区域；
- 所有区域复用同一应用采集源与输入协议，只增加裁剪和坐标偏移；
- 区域配置持久化，并以 bundle identifier 识别应用；
- 为被添加为“快捷 App”的应用保存独立 App Profile。

### V1 不做

- 自研 VPN、路由盒子或硬件；
- 自研新的音视频编码器或底层传输协议；V1 应复用现成协议和系统采集能力；
- Windows、Linux、Android 原生客户端；
- 多用户、团队权限和云同步；
- 基于视觉模型的全自动界面切分；
- 为每个第三方应用开发专用 UI；
- 无人确认的高风险系统操作。

这些功能现在做只会把验证周期拖烂。V1 只回答一个问题：在完整远控能力仍然存在的前提下，用户是否愿意把大多数高频操作切换到快捷 App 模式。

## 4. 目标用户与首个场景

### 首批用户

- 经常在 Mac 上运行 Codex、Claude Code 或其他 agent 的开发者；
- 任务运行时间长，需要离开电脑后继续确认、补充或查看状态；
- 已经使用 Tailscale、ZeroTier 或其他远程组网工具；
- 能接受安装 Mac 常驻服务并授予辅助功能权限。

### 首个验证场景：Codex

预置或由用户创建以下动作：

1. 查看当前任务状态；
2. 发送一段文本；
3. 点击确认或继续；
4. 停止当前任务；
5. 打开 Codex 窗口；
6. 进入完整远程控制兜底。

Codex 只是切入口，不应被写死在底层模型里。

## 5. 核心交互

### 创建交互区域

1. 用户选择目标应用；
2. 手机只显示该应用的完整实时画面；
3. 用户在完整 App 画面上框选区域；
4. 设置区域名称并保存；
5. 手机把每个区域渲染成独立的实时操作面；
6. 区域内手势通过坐标变换回送到同一个应用窗口。

V1 持久化窗口内归一化坐标，禁止保存桌面绝对像素。Accessibility 锚点和视觉重定位用于后续增强区域稳定性，不改变区域是实时操作面的主体模型。

### 手机端执行动作

1. 首页展示 Mac 在线状态与动作卡片；
2. 用户点击普通动作，直接执行；
3. 输入类动作先打开轻量输入框；
4. 高风险动作要求二次确认；
5. 返回明确结果：成功、超时、目标未找到、权限不足或应用不在线；
6. 目标失效时提示用户回到 Mac 重新绑定。

### 逐级展开

应用选择、交互区域、完整应用和完整桌面不是四套割裂功能，而是同一个远控会话的四种呈现层级：

```text
选择应用
    → 多个实时交互区域
        → 完整应用窗口
            → 完整电脑桌面
```

用户可以随时向上展开，也可以在完成复杂操作后收回快捷模式。快捷配置失效时，系统应自动提示并提供完整应用窗口，不能把用户扔进死路。

### 快捷 App

没有配置的普通 App 仍然支持完整桌面和窗口远控。用户将某个 App 添加为快捷 App 后，系统为它建立一份 App Profile：

```text
App Profile
├── 应用、进程与窗口识别规则
├── 需要持续展示的局部区域
├── 实时交互区域与手机端排序
├── 视觉锚点与 Accessibility 定位器
├── 手机端布局
└── 展开到完整 App / 桌面的降级策略
```

## 6. 动作模型

动作不应等同于一个屏幕坐标。建议的数据结构：

```text
Action
├── id
├── name
├── icon
├── targetApp
│   ├── bundleIdentifier
│   └── optionalExecutablePath
├── targetWindow
│   ├── titleMatcher
│   └── role
├── locator[]
│   ├── accessibilityPath
│   ├── role
│   ├── identifier
│   ├── label
│   ├── relativePosition
│   └── optionalImageTemplate
├── operation
│   ├── press
│   ├── setValue
│   ├── keyboardShortcut
│   ├── openApplication
│   └── sequence
├── inputSchema
├── confirmationPolicy
├── timeout
└── lastValidationResult
```

定位器需要按可靠性排序并允许降级：

1. Accessibility identifier；
2. 可访问性层级、角色与标签组合；
3. 窗口内相对位置；
4. 图像模板或 OCR；
5. 绝对坐标，仅作最后兜底。

## 7. 技术架构

```text
手机 Web/PWA
    │ HTTPS / WebSocket
    ▼
本地控制服务
    ├── 设备认证与会话管理
    ├── 完整桌面 / 应用窗口 / 局部区域视图
    ├── App Profile 与手机布局
    ├── 动作目录
    ├── 执行队列
    ├── 状态与结果推送
    └── 审计日志
            │
            ▼
远程控制核心
    ├── 桌面、窗口和区域画面采集
    ├── 视频帧编码与传输
    ├── 键盘、鼠标和触控输入注入
    ├── 窗口与进程寻址
    └── 会话、重连和权限
            │
            ▼
交互增强层
    ├── Accessibility API
    ├── Apple Events / Shortcuts
    ├── 视觉锚点与 OCR
    ├── 局部截图与元素校验
    └── 可选 Computer Use 识别与修复
```

### 推荐实现

- Mac 客户端：Swift + SwiftUI；
- 画面采集：优先使用 ScreenCaptureKit，统一支持桌面、窗口和区域裁剪；
- 输入注入：CGEvent，并保留面向指定应用/进程的后台输入研究路线；
- 语义交互：AXUIElement，必要时调用 AppleScript/Shortcuts；
- 视频与传输：先复用 WebRTC 或成熟远控组件，不要自己发明编码和拥塞控制；
- 手机端：React 18 + TypeScript + Vite 的响应式 PWA，避免一开始维护原生 iOS 客户端；
- Web 视觉与基础组件：直接复用 Big Minds 的 Tailwind 4 + Radix/shadcn 设计系统，抽取进当前仓库并独立版本化，不建立本机绝对路径依赖；
- 通信：本地 HTTPS + WebSocket；
- 数据：SQLite 或本地结构化文件，敏感字段进入 Keychain；
- 远程网络：先依赖用户已有的 Tailscale 等私网，不把公网暴露作为默认能力；
- 配对：Mac 显示一次性二维码，手机扫码交换设备公钥和短期配对令牌。

不要用 Electron 做 Mac 执行核心。它能做界面，但系统辅助功能、权限管理和稳定的后台执行最终仍会逼你写原生代码。

## 8. 安全设计

这是能操作用户电脑的工具，安全不是以后补的装饰。

- 默认只监听本机或指定私网接口；
- 不允许匿名访问；
- 手机与 Mac 使用设备级密钥配对；
- 所有命令包含随机数、时间戳和过期时间，防止重放；
- 高风险动作必须二次确认；
- 密码输入框、安全输入区和系统授权窗口默认禁止自动化；
- 每次执行记录来源设备、动作、时间、结果，不记录敏感输入正文；
- 提供一键暂停远程执行和撤销设备授权；
- 执行动作时 Mac 菜单栏显示明显状态；
- 不提供任意脚本远程执行能力，至少 V1 不提供。

## 9. 里程碑

### 阶段 0：技术探针，3—5 天

目标：证明核心系统能力可行，不做漂亮界面。

- 枚举前台应用、窗口和可访问性树；
- 高亮鼠标指向的可交互元素；
- 抓取完整桌面、指定窗口和指定区域画面；
- 对指定窗口或区域注入点击、滚动和文本输入；
- 保存元素定位信息；
- 应用重启、窗口移动后重新找到元素；
- 执行点击、文本输入和快捷键；
- 验证“快捷控件 → 局部画面 → 完整窗口 → 完整桌面”的展开链路；
- 用 Codex 完成至少三个真实动作。

退出条件：同一动作在应用重启后仍能稳定执行。做不到就先解决定位，别急着写手机端。

### 阶段 1：本地 MVP，1—2 周

- Mac 菜单栏应用；
- 抽取 Big Minds 设计 token、主题与通用 UI primitives；
- 动作创建、编辑、删除、测试；
- 动作卡片与分组；
- 快捷 App 创建与 App Profile 保存；
- 完整桌面、应用窗口与局部区域视图；
- 本机 Web 控制页；
- 执行结果和错误分类；
- 基础审计日志。

退出条件：用户在同一局域网内，可以从手机稳定执行 5 个自定义动作。

### 阶段 2：远程可用，1—2 周

- 二维码配对；
- 私网环境下的 HTTPS 与 WebSocket；
- Mac 在线状态、断线重连和命令超时；
- 动作级确认策略；
- 在同一会话内逐级展开到完整桌面；
- 安装、权限申请和故障诊断流程。

退出条件：连续一周日常使用中，无需手动修复链路；失效动作能够被准确识别，而不是误点其他位置。

### 阶段 3：小规模验证，2—4 周

- 邀请 5—10 名真实用户；
- 记录动作创建成功率、复用率和失败原因；
- 优化最常见的定位与权限问题；
- 提供 Codex 示例动作包，但仍允许用户自行修改；
- 判断是否值得做原生手机端。

退出条件见下一节。数据不达标就停，不要靠继续堆功能自我感动。

## 10. 验证指标

### 核心指标

- 至少 50% 测试用户在第二周仍主动使用；
- 活跃用户每周执行至少 10 次快捷动作；
- 至少 60% 的远程操作通过快捷动作完成，而不是打开远程桌面；
- 已保存动作的成功执行率达到 95%；
- 创建第一个可用动作的中位时间低于 3 分钟；
- 元素失效时误操作率接近 0，宁可报错也不能点错。

### 需要记录的失败原因

- 应用或窗口未运行；
- 权限被撤销；
- 元素定位失败；
- 定位到多个候选元素；
- 输入焦点错误；
- 网络断开或超时；
- 应用版本变化导致动作失效；
- 用户最终改用完整远程桌面。

## 11. 主要风险与处理

### 可访问性树质量不稳定

部分 Electron、Canvas、游戏或自绘界面不会暴露可靠元素。处理顺序是可访问性定位、相对位置、局部图像匹配，最后才是坐标。每次执行前应校验目标，无法确认就拒绝执行。

### 用户创建动作太麻烦

如果用户每次都要研究层级和定位器，这产品就是失败。桌面端必须做到悬停高亮、点击选择、立即测试。后续再通过社区模板和自动检测降低成本。

### 官方客户端覆盖核心需求

不要与 Codex 官方客户端拼完整聊天体验。差异必须是跨应用、组合动作和本机工作流。如果用户只操作 Codex，一个第三方遥控层很难赢。

### 系统权限与分发

辅助功能、自动化和屏幕录制权限会显著增加安装摩擦。应尽早测试签名、公证、权限说明和升级后的权限保持，别等产品做完才发现安装流程劝退所有人。

### 远程执行造成安全事故

默认拒绝模糊目标和敏感区域，限制动作能力，保留审计与撤权入口。执行“看起来差不多”的目标是最蠢也最危险的容错方式。

## 12. 后续演进条件

只有 V1 指标成立后才进入以下方向：

- 自动扫描并推荐可交互元素；
- OCR 与视觉模型辅助定位；
- 多步工作流与条件分支；
- 动作模板分享和版本适配；
- 原生 iOS/Android 客户端；
- Windows 执行端；
- 多电脑切换；
- 团队权限和云同步；
- 状态提取与局部信息卡片，而不只是动作按钮。

硬件路由盒子只有在大量用户明确因网络配置无法使用产品时才值得重新评估。否则它只是昂贵、低毛利、售后麻烦的跑偏方向。

## 13. 立即执行清单

1. 初始化一个最小 macOS Swift 项目；
2. 做 Accessibility 权限申请与元素枚举；
3. 完成“选择元素—保存—重启应用—再次点击”的闭环；
4. 选择 Codex 中三个操作作为测试用例；
5. 记录定位成功率与失效原因；
6. 技术探针通过后，再开发手机页面和远程通信。

第一阶段最重要的交付物不是 UI，而是一份可靠性结果：哪些应用能稳定定位，哪些不能，失败后是否能够安全停止。
