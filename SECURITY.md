# Security Policy

## 部署边界

LAN 模式使用 bearer token，但当前默认是明文 HTTP，只适合可信局域网。不要把服务直接暴露到公网，也不要把 token 写进 issue、日志或截图。

## 报告漏洞

请不要公开提交可被利用的安全问题。优先通过仓库维护者提供的私下联系方式报告，并附上复现步骤、影响范围和修复建议。没有私下渠道时，先提交不包含利用细节的 issue，注明需要私下沟通。

## 支持版本

目前只保证 `main` 分支最新代码。项目仍在 MVP 阶段，尚未承诺公网部署安全性或长期兼容性。
